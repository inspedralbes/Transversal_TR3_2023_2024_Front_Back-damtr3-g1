version: '3'

services:
  

  webServer:
    build: 
      context: .
      dockerfile: Dockerfile.nodejs
    ports:
      - "3170:3170"
    volumes:
      - ./:/app
    command: node webServer.js
    restart: always
